import { WorkspaceLibrary } from '../components/workspace-library/WorkspaceLibrary';

/** Home: calm workspace library (canvas “desk” lives at `/desk`). */
export function Dashboard() {
  return <WorkspaceLibrary />;
}
